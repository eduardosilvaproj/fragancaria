// Dummy Next.js config to satisfy Vercel's framework detector.
// The actual build is handled by Vite + TanStack Start + Nitro (vercel preset).
// This file exists ONLY to prevent "No Next.js version detected" error.
// Vercel will use our custom buildCommand and outputDirectory instead.

export default {};
